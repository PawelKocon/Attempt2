package controller;


import java.util.Vector;

public class Controller {
    public static void main(String[] args) {
        //przygotowanie danych do przekazania do metod klas.

            Vector<Double> tab=new Vector<Double>();
            /*
            * uzywamy kontenerów zamiast tablic, kontener budowa kontener
            * to klasa do przechowywania iluś elementów
            *
            *
            * */
            for (int i = 0; i< args.length-1; i++)
            {
                    tab.add(Double.parseDouble())
            }
    }
}
